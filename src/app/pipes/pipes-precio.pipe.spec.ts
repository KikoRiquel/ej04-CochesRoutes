import { PipesPrecioPipe } from './pipes-precio.pipe';

describe('PipesPrecioPipe', () => {
  it('create an instance', () => {
    const pipe = new PipesPrecioPipe();
    expect(pipe).toBeTruthy();
  });
});
